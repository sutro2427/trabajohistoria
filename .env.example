# ============================================================================
# Configuración de Firebase — copiar a .env.local (desarrollo) o pegar en
# Netlify → Site settings → Environment variables (producción)
# ============================================================================
#
# Estas claves NO son secretas: viajan dentro del paquete que se descarga el
# navegador y están pensadas para ser públicas. Lo que protege los datos son
# las reglas de firestore.rules, no ocultar esta configuración.
#
# Sin estas variables el juego arranca igualmente en modo local: se puede
# jugar la campaña completa, solo que sin panel compartido.
#
# Dónde encontrarlas: consola de Firebase → ⚙ Configuración del proyecto →
# Tus aplicaciones → Aplicación web → Configuración de SDK.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=tu-proyecto.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=tu-proyecto
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=

# Contraseña del panel del profesor. OPCIONAL: si se deja vacía se usa la que
# viene por defecto en el código (RONKAGEI). Se entra con ?admin en la URL y el
# panel la pide; también vale ?admin=LA-CLAVE para no tener que escribirla.
#
# Es una barrera de conveniencia, no de seguridad: se comprueba en el navegador.
# Lo que protege los datos de verdad son las reglas de firestore.rules.
VITE_ADMIN_KEY=
